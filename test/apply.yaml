a:
    from:
        type: docker
        url: docker://centos:latest
    run: |
        touch /a
        echo "hello" > /foo
b:
    from:
        type: docker
        url: docker://centos:latest
    run: |
        touch /b
        echo "world" > /foo
both:
    from:
        type: docker
        url: docker://centos:latest
    run: cat /foo
    apply:
        - oci:oci:a
        - oci:oci:b
