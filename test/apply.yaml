a:
    from:
        type: docker
        url: docker://centos:latest
    run: touch /a
b:
    from:
        type: docker
        url: docker://centos:latest
    run: touch /b
both:
    from:
        type: docker
        url: docker://centos:latest
    run: echo hello world
    apply:
        - oci:oci:a
        - oci:oci:b
